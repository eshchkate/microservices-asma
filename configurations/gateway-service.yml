server:
  port: 8060

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

zuul:
  routes:
    client:
      path: /client/**
 #     serviceId: config-client
      url: http://loclahost:8981/config-client/client/
