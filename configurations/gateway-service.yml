server:
  port: 8060

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

zuul:
  routes:
    student-service:
      path: /student-api/**
      serviceId: STUDENT-SERVICE