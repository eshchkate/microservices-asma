server:
  port: 8081

ribbon:
  eureka:
    enabled: true

hystrix:
  metrics:
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include=*:

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka