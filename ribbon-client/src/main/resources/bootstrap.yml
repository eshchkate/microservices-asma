spring:
  application:
    name: ribbon-client
  cloud:
    config:
      uri: http://localhost:8980
  main:
    allow-bean-definition-overriding: true
  zipkin:
    base-url: http://localhost:9411/
  sleuth:
    sampler:
      probability: 1
server:
  port: 8088

